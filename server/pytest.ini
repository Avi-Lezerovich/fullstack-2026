[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: pure logic and single-service tests
    integration: HTTP tests through the Flask test client
    worker: scheduler tick and full-trial tests
filterwarnings =
    error::DeprecationWarning:app.*
    error::DeprecationWarning:worker.*
