Flask>=3.0
flask-cors>=4.0
bcrypt>=4.0
PyMySQL>=1.1
gunicorn>=21.2
# The live brain. Optional at runtime - without it the bots fall back to the
# deterministic offline generator - but required for LLM_PROVIDER=bedrock.
anthropic[bedrock]
