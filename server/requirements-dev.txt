# Test-only dependencies (unit + integration tests).
# Deliberately kept OUT of requirements.txt so they are NOT baked into the
# production Docker image — the prod image should ship runtime deps only.
#
#   py -m pip install -r requirements-dev.txt   (Windows: use the `py` launcher)
#
pytest>=8.0
pytest-cov>=5.0
