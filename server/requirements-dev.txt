-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
# Optional: only needed to exercise the live Claude backend. brain/claude.py
# imports it lazily, so everything works without it and the tests never
# require it (they force the offline generator).
# anthropic>=0.40
